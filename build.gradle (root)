buildscript {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    dependencies {
        // Android Gradle Plugin version compatible with Gradle 8.x / AGP 8.1.x
        classpath "com.android.tools.build:gradle:8.1.0"
        // Kotlin Gradle plugin (if you use Kotlin in app module)
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
